name: Setup tools
description: Install tools for the project using Aqua
runs:
  using: composite
  steps:
    - uses: aquaproj/aqua-installer@v3.0.1
      with:
        aqua_version: v2.29.0
    - run: aqua i -l
      shell: bash