apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
spec:
  interval: 5m
  chart:
    spec:
      chart: kube-prometheus-stack
      version: '87.16.1'
      sourceRef:
        kind: HelmRepository
        name: prometheus
      interval: 5m
  values: 
    crds:
      # enable installing and upgrading CRDs
      enabled: true
      upgradeJob:
        enabled: true
        forceConflicts: true
    grafana:
      defaultDashboardsTimezone: Asia/Riyadh
      envFromSecret: grafana-env
      # Use an existing secret for the admin user.
      admin:
        ## Name of the secret. Can be templated.
        existingSecret: "grafana-admin"
        userKey: admin-user
        passwordKey: admin-password
      ingress:
        enabled: true
        ingressClassName: cilium
        hosts:
          - grafana.almothana.xyz
        tls:
          - secretName: almothana-xyz-tls
            hosts:
            - grafana.almothana.xyz
      persistence:
        enabled: true
        type: sts
        accessModes:
          - ReadWriteOnce
        size: 5Gi
        finalizers:
          - kubernetes.io/pvc-protection
    prometheus:
      ingress:
        enabled: true
        ingressClassName: cilium
        pathType: Prefix
        hosts:
          - prometheus.almothana.xyz
        tls:
          - secretName: almothana-xyz-tls
            hosts:
            - prometheus.almothana.xyz
      prometheusSpec:
        # set as false to use the rules in the PrometheusRule resources (https://github.com/onzack/grafana-dashboards/tree/main?tab=readme-ov-file#troubleshooting)
        ruleSelectorNilUsesHelmValues: false
        # testing for CNPG issues: https://github.com/cloudnative-pg/charts/issues/279#issuecomment-2130308807
        podMonitorSelectorNilUsesHelmValues: false
        serviceMonitorSelectorNilUsesHelmValues: false
        probeSelectorNilUsesHelmValues: false
        # configure remote write to the Openobserve instance
        # remoteWrite:
        #   - url: https://openobserve.almothana.xyz/api/default/prometheus/api/v1/write
        #     name: openobserve
        #     queueConfig:
        #       maxSamplesPerSend: 1000
        #     basicAuth:
        #       username:
        #         name: openobserve-remote-write
        #         key: username
        #       password:
        #         name: openobserve-remote-write
        #         key: password
        storageSpec: 
          volumeClaimTemplate:
            spec:
              accessModes: ["ReadWriteOnce"]
              resources:
                requests:
                  storage: 50Gi
    alertmanager:
      alertmanagerSpec:
        secrets:
          - slack-secret
          - smtp-secret
        storage:
          volumeClaimTemplate:
            spec:
              accessModes: ["ReadWriteOnce"]
              resources:
                requests:
                  storage: 1Gi
      config:
        global:
          resolve_timeout: 5m
          smtp_smarthost: 'mail.almothana.sa:587'
          smtp_from: 'no-reply@almothana.sa'
          smtp_auth_username: 'no-reply@almothana.sa'
          smtp_auth_password_file: /etc/alertmanager/secrets/smtp-secret/password
          smtp_require_tls: true
        route:
          group_by: ['namespace']
          group_wait: 2m
          group_interval: 10m
          repeat_interval: 48h
          receiver: 'slack'
          routes:
            - receiver: 'null'
              matchers:
                - alertname = "Watchdog"
            - receiver: 'slack'
              matchers:
                - severity = 'critical'
        receivers:
        - name: "null"
        - name: 'slack'
          slack_configs:
          - api_url_file: /etc/alertmanager/secrets/slack-secret/slack-api-url
          email_configs:
          - to: 'alerting@almothana.sa'
            send_resolved: true

