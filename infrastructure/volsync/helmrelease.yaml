apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: volsync
spec:
  interval: 5m
  chart:
    spec:
      chart: volsync
      version: '0.16.0'
      sourceRef:
        kind: HelmRepository
        name: backube
      interval: 5m
  values:
    metrics:
      disableAuth: true