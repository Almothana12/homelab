apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: volsync
spec:
  interval: 5m
  chart:
    spec:
      chart: backube
      version: '0.15.0'
      sourceRef:
        kind: HelmRepository
        name: volsync
      interval: 5m
  # values: 