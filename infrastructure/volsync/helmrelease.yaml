apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: volsync
spec:
  interval: 5m
  chart:
    spec:
      chart: volsync
      version: '0.15.0'
      sourceRef:
        kind: HelmRepository
        name: backube
      interval: 5m
  # values: 