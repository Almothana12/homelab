apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istio-base
spec:
  interval: 5m
  timeout: 10m
  chart:
    spec:
      chart: base
      version: "1.30.5"
      sourceRef:
        kind: HelmRepository
        name: istio
      interval: 24h
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace