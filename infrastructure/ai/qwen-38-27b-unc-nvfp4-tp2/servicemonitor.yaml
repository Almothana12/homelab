apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vllm-unc-tp
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: vllm-unc-tp
  endpoints:
    - port: http
      path: /metrics
      interval: 5s
      scrapeTimeout: 4s
