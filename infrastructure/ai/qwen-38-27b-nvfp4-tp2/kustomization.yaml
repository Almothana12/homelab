apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai
resources:
  - leaderworkerset.yaml
  - service.yaml
  - PVC.yaml
  - servicemonitor.yaml
configMapGenerator:
  - name: vllm-tp-bootstrap
    files:
      - bootstrap.py
    options:
      immutable: true
configurations:
  - kustomizeconfig.yaml
