apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: k8s-mechanic
spec:
  interval: 1h
  chart:
    spec:
      chart: mechanic
      version: '0.4.4'
      sourceRef:
        kind: HelmRepository
        name: k8s-mechanic
        namespace: mechanic
  values:
    gitops:
      repo: Almothana12/homelab
      manifestRoot: "."