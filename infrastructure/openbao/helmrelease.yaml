apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openbao
spec:
  interval: 5m
  chart:
    spec:
      chart: openbao
      version: '0.28.4'
      sourceRef:
        kind: HelmRepository
        name: openbao
      interval: 5m
  # values: 
