apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: default-l2
spec:
  interfaces:
    # PCI interface names can use lowercase p or uppercase P (for example enP7s7).
    - ^en[pP].*
    - ^eth[0-9]+
  externalIPs: true
  loadBalancerIPs: true
