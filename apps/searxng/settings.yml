use_default_settings: true

general:
  instance_name: "SearXNG"

search:
  formats:
    - html
    - json

server:
  base_url: "https://searxng.almothana.xyz/"
  image_proxy: true
  # secret_key is injected via $SEARXNG_SECRET from the searxng Secret.

